{
  "name": "theophysics-canonization",
  "version": "2.0.0",
  "description": "Shared candidate canonization engine, Obsidian client, and governed review workbench.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build:registry": "tsx tools/build-web-registry.ts",
    "build": "npm run build:registry && tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "lint": "eslint src tests --ext .ts",
    "typecheck": "tsc -noEmit -skipLibCheck",
    "test": "tsx --test tests/**/*.test.ts",
    "verify": "npm run build:registry && npm run typecheck && npm run lint && npm test && npm run build",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "semantic",
    "ai",
    "tagging",
    "classification",
    "knowledge-management"
  ],
  "author": "YellowKidokc",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.14.0",
    "@typescript-eslint/parser": "^6.14.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "0.19.11",
    "eslint": "^8.57.1",
    "obsidian": "latest",
    "tslib": "^2.6.2",
    "tsx": "^4.20.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "uuid": "^9.0.1"
  }
}
