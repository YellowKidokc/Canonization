# Standalone remaining gaps

1. Obsidian desktop was not exposed as a command or installed application in this environment. The disposable vaults were built and statically inspected, but visible plugin enablement, ribbon interaction, command execution, browser launch, file-picker use, and independent disable/re-enable remain manual runtime checks.
2. Neutral discovery currently captures the exact source hash and immutable source-only discovery envelope, while provider results are proposals. Real provider calls require a user-supplied Canonization-owned credential; no credential was created, copied, or exercised.
3. Browser-to-Obsidian return requires a manual exported-JSON file selection. This is documented and preserves the governed JSON authority boundary.
4. `npm audit` reports 12 development/dependency-tree advisories (2 moderate, 10 high), including legacy lint/build dependencies and major-version fixes for esbuild and uuid. They were not automatically upgraded because dependency upgrades were outside this fork correction.
5. Historical implementation symbols beginning with `SemanticAI` remain internal. Historical migration receipts, previous release-package receipts, and byte-preserved Nerve inputs remain unchanged as provenance.
