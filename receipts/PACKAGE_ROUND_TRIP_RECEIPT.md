# Package round-trip receipt

The automated storage-neutral test exports the reviewed fixture, validates checksums/schema/IDs, imports into an empty disposable-vault state, preserves its unknown extension, and regenerates identical Markdown. This is an automated adapter-level round trip, not a claim that an Obsidian UI was operated.
