# Copy to .env and fill in real values. .env is gitignored and must never be committed.

# Embedded PostgreSQL (genuine Postgres via pgserver, localhost-only bind).
PG_HOST=127.0.0.1
PG_PORT=55432
PG_USER=canonization
PG_PASSWORD=<generate-a-strong-password>
PG_DATABASE=canonization
PG_DATA_DIR=C:\Users\David\AppData\Local\Theophysics\Canonization\postgres-data
# DATABASE_URL_OVERRIDE=postgresql://user:pass@host:5432/db   # optional: real server swap

# Auth
CANONIZATION_PASSWORD=<your-login-password>
SESSION_SECRET=<generate-a-long-random-secret>

# AI (server-side only)
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat

# Exchange / projections
EXCHANGE_DIR=C:\Users\David\Documents\faiththruphysics.com\60_EXCHANGE
